#pragma once

#include "Player.h"
#include "vector"

/*class Visitor;

class Game {
    list<shared_ptr<PlayerUser>> _users;
    list<shared_ptr<PlayerComp>> _comps;
public:
    Game(const int, const int);
    ~Game(void) {};
    bool start(shared_ptr<Visitor>);

};*/